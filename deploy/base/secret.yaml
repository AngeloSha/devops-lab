# PLACEHOLDER ONLY — this repo is public and this value is intentionally fake.
# Real secrets must never be committed in plain text. The production-grade
# options are Sealed Secrets, SOPS, or External Secrets Operator (future round).
apiVersion: v1
kind: Secret
metadata:
  name: devops-lab-secret
type: Opaque
stringData:
  demo-secret: "not-a-real-secret"
